@page "/"

<a class="btn btn-success" href="personal/create"><i class="bi bi-plus"></i> Crear personal nuevo</a>
<hr />

<ListPersonalComponents></ListPersonalComponents>

<style>

    .overlay {
        position: fixed;
        top: 0;
        left: 0;
        bottom: 0;
        right: 0;
        background: rgba(0,0,0,.7);
    }

    .overlay__wrapper {
        width: 100%;
        height: 100%;
        position: relative;
        text-align: center;
    }

    .overlay__spinner {
        position: absolute;
        left: 50%;
        top: 50%;
        transform: translate(-50%, -50%);
    }

    .spinner-border{
        width: 5rem;
        height: 5rem;
    }

    .swal2-container {
        z-index:9999;
    }
}

@@keyframes spin {
    0% {
        transform: rotate(0deg)
    }

    100% {
        transform: rotate(360deg)
    }
}

    .table-container {
        width: 100%;
        height: 33rem;
        overflow: auto;
    }

    .table-wrapper {
        position: relative;
    }

    .table {
        width: auto;
    }

    thead th {
        position: sticky;
        top: 0;
        background-color: white;
        z-index: 2;
    }

        thead th:first-child,
        thead th:last-child {
            background-color: transparent;
        }

    tbody th,
    tbody td {
        border-top: 1px solid #ddd;
    }

</style>

@code {

}